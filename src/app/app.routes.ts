import { Routes } from '@angular/router';
import { BoxSelectionObservablesComponent } from './interactive-box-selection/box-selection/box-selection.component';

export const routes: Routes = [
    { path: '', component: BoxSelectionObservablesComponent }
];
